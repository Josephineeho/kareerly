export interface HeroSidePicProps {
    img:string,
    descriptor: string,
    description: string
}

export interface SignUpProps {
    full_name: string;
    password: string;
    email: string;
    role: string;
    company_name?: string;
    industry?: string;
}

